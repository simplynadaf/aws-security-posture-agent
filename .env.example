# Sentry DSN - get from https://sentry.io after signup with code bugsmash26
SENTRY_DSN=

# AWS (uses ~/.aws/credentials by default, but can override here)
AWS_DEFAULT_REGION=us-east-1

# Bedrock Model
MODEL=bedrock/amazon.nova-pro-v1:0
